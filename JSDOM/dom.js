// The Document Object Model (DOM) is an application programming interface(API) for 
// Manipulating HTML and XML documents. The DOM represents a document as a tree of nodes.
// It defines the logical structure of documents and the way of a document is accessed and manipulated

function add()
{
    
}